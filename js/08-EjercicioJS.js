
let numero = parseInt(prompt("Introduce un número:"));


if (numero % 2 == 0) {
    alert("El número " + numero + " es divisible por 2 (es par).");
} else {
    document.write("El número " + numero + " no es divisible por 2 (es impar).");
}
